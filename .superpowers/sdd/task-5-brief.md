### Task 5: Final verification

**Files:** none new

- [ ] **Step 1: Typecheck**

Run: `npx tsc --noEmit`

Expected: exit 0 (or only pre-existing errors unrelated to these files â€” fix any new errors introduced here).

- [ ] **Step 2: Full smoke checklist (from spec)**

1. `/dfy` â€” tutorial under hero; video plays; only one tutorial.
2. `/instant`, `/autopilot`, `/hot-threads` â€” placeholder, not playable.
3. Autopilot + Hot Threads errors scroll into view.
4. Instant build or Search error scrolls into view.
5. Auth error scrolls into view.

- [ ] **Step 3: Commit only if Step 1â€“2 required fixes**

Otherwise no extra commit.

---

